import { FollowerTextPipe } from './follower-text.pipe';

describe('FollowerTextPipe', () => {
  it('create an instance', () => {
    const pipe = new FollowerTextPipe();
    expect(pipe).toBeTruthy();
  });
});
