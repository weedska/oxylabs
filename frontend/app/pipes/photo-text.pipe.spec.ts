import { PhotoTextPipe } from './photo-text.pipe';

describe('PhotoTextPipe', () => {
  it('create an instance', () => {
    const pipe = new PhotoTextPipe();
    expect(pipe).toBeTruthy();
  });
});
