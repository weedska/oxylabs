import { DateFormatAgoPipe } from './date-format-ago.pipe';

describe('DateFormatAgoPipe', () => {
  it('create an instance', () => {
    const pipe = new DateFormatAgoPipe();
    expect(pipe).toBeTruthy();
  });
});
